## Phase 1 – Money Transfer CLI Application

This is a **Command-Line Interface (CLI)** application for performing secure money transfers using the **CliQ approach**. The application allows users to transfer money using **Mobile Numbers, Aliases, or IBANs**, with proper validation and account management.

The application was implemented using **Visual Studio Code** and built with a **Maven environment**.

The application can be **built and run using Docker**.
